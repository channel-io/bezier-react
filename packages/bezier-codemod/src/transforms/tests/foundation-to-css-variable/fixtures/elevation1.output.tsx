const div = styled.div`
  background-color: var(--bg-white-low);
  box-shadow: var(--ev-1);
`

const div = styled.div`
  background-color: var(--bg-white-low);
  box-shadow: var(--ev-2);
`

const div = styled.div`
  background-color: var(--bg-white-high);
  box-shadow: var(--ev-3);
`

const div = styled.div`
  background-color: var(--bg-white-high);
  box-shadow: var(--ev-4);
`

const div = styled.div`
  background-color: var(--bg-white-high);
  box-shadow: var(--ev-5);
`

const div = styled.div`
  background-color: var(--bg-white-high);
  box-shadow: var(--ev-6);
`
