const div = styled.div`
  box-shadow: var(--ev-1);
`

const div = styled.div`
  box-shadow: var(--ev-1);
`

const div = styled.div`
  box-shadow: var(--ev-1);
`

const div = styled.div`
  box-shadow: var(--ev-1);
`

const div = styled.div`
  ${({ foundation, showNavigation }) =>
  !showNavigation && foundation?.elevation?.ev3()};
`
