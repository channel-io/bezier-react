{
  "$schema": "https://turborepo.org/schema.json",
  "baseBranch": "origin/next-v1",
  "pipeline": {
    "build": {
      "outputs": [
        "build/**"
      ],
      "dependsOn": [
        "^build"
      ]
    },
    "lint": {
      "outputs": [
        ".eslintcache",
        ".stylelintcache"
      ]
    },
    "test": {
      "outputs": [
        "coverage/**"
      ],
      "inputs": [
        "src/**/*.tsx",
        "src/**/*.ts",
        "src/**/*.js"
      ]
    },
    "dev": {
      "cache": false
    },
    "clean": {
      "cache": false
    }
  }
}
