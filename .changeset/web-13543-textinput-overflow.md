---
'@channel.io/bezier-react': patch
---

Reveal overflowing text while horizontally scrolling beta `TextInput` and `Search`, and restore ellipsis near the starting position without changing the input value, selection, or native scroll position.
