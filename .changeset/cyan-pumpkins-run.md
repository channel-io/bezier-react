---
"@channel.io/bezier-react": major
---

Changes with interface of `Tag` and `Badge` component.

- They do not support `interpolation` props any more.
- They support margin related props such as `m`, `mx`, `my`, `mt`, and so on.
